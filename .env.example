# CDNLite environment template
# Copy to .env and adjust values if needed.

# Optional image owner prefix for local image tags.
GITHUB_REPOSITORY_OWNER=local

# PostgreSQL container bootstrap
POSTGRES_DB=cdnlite
POSTGRES_USER=cdnlite
POSTGRES_PASSWORD=cdnlite

# Core database connection
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=cdnlite
DB_USERNAME=cdnlite
DB_PASSWORD=cdnlite

# Core logging
APP_LOG_ENABLED=1
APP_LOG_LEVEL=info
APP_DEBUG=0

# Edge identity and auth
EDGE_ID=edge-local-1
EDGE_TOKEN=edge-dev-token
EDGE_HOSTNAME=edge-local-1
# Use auto to let edge-agent discover the public IPv4 address on register and heartbeat.
EDGE_PUBLIC_IP=auto
EDGE_REGION=local
EDGE_VERSION=v1

# Host-exposed service ports
CORE_HOST_PORT=8080
EDGE_HOST_PORT=8081
POSTGRES_HOST_PORT=5432

# Edge agent runtime
CORE_URL=http://core:8080
EDGE_CONFIG_PATH=/var/lib/cdnlite/config.json
METRIC_PATH=/var/lib/cdnlite/metrics.ndjson

# PowerDNS sync (optional)
# 0/1 toggle. When enabled, core syncs DNS record create/delete to PowerDNS.
POWERDNS_ENABLED=0
# If 1, DNS API/CLI calls fail when PowerDNS sync fails.
POWERDNS_STRICT=0
POWERDNS_API_URL=http://powerdns:8081
POWERDNS_API_KEY=change-me
POWERDNS_SERVER_ID=localhost
POWERDNS_ZONE_KIND=NATIVE
POWERDNS_ZONE_NAMESERVERS=ns1.local.
