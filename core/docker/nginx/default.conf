server {
    listen 8080;
    server_name _;
    root /app;
    index public_index.php;

    client_max_body_size 2m;
    client_body_timeout 15s;
    keepalive_timeout 30s;
    send_timeout 30s;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "no-referrer" always;

    access_log /dev/stdout;
    error_log /dev/stderr warn;

    location ~ /\.(?!well-known).* {
        deny all;
    }

    location ~ ^/(app|database|docker|storage|vendor|tests|bootstrap|\.git)(/|$) {
        deny all;
    }

    location ~ \.(?:env|ini|sql|sqlite|log|sh|md|lock|json)$ {
        deny all;
    }

    location / {
        rewrite ^ /public_index.php last;
    }

    location = /public_index.php {
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /app/public_index.php;
        fastcgi_param SCRIPT_NAME /public_index.php;
        fastcgi_param HTTP_AUTHORIZATION $http_authorization;
        fastcgi_read_timeout 60s;
        fastcgi_send_timeout 60s;
        fastcgi_connect_timeout 5s;
        fastcgi_pass 127.0.0.1:9000;
    }

    location ~ \.php$ {
        return 404;
    }
}
