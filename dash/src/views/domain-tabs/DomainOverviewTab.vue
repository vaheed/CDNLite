<template><div class="grid gap-4 md:grid-cols-2"><div class="card p-5"><p class="text-sm text-slate-500">Domain status</p><p class="mt-2 font-bold">{{ domain.status ?? 'unknown' }}</p></div><div class="card p-5"><p class="text-sm text-slate-500">Nameservers</p><p class="mt-2 font-bold">{{ domain.nameserver_status ?? 'unknown' }}</p></div><p class="text-sm text-slate-500 md:col-span-2">Configure origins, proxying, TLS verification, and geo origins in the DNS tab.</p></div></template>
<script setup lang="ts">import type { Domain } from '@/types'; defineProps<{ domain: Domain }>();</script>
