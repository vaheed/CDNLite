<template><DomainRulesTab :domain-id="domainId" title="Page Rules" summary="Apply edge behavior to matching paths." :fields="fields" :columns="columns" :list="() => pageRulesApi.list(domainId)" :create="(v) => pageRulesApi.create(domainId, v)" :update="(id, v) => pageRulesApi.update(domainId, id, v)" :remove="(id) => pageRulesApi.remove(domainId, id)" /></template>
<script setup lang="ts">import DomainRulesTab from './DomainRulesTab.vue'; import { pageRulesApi } from '@/lib/api/pageRules'; defineProps<{ domainId:string }>(); const fields=[{key:'enabled',label:'Enabled',type:'checkbox' as const,default:true},{key:'pattern',label:'Pattern',default:'/*'},{key:'priority',label:'Priority',type:'number' as const,default:100},{key:'actions',label:'Actions JSON',type:'textarea' as const,default:'{}'}]; const columns=[{key:'enabled',label:'Enabled'},{key:'pattern',label:'Pattern'},{key:'priority',label:'Priority'},{key:'actions',label:'Actions'}];</script>
